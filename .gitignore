# Large VM artifacts — never commit these (GitHub rejects files > 100 MB)
*.vdi
*.vhd
*.qcow2
*.iso
*.img
*.raw

# QEMU binaries (re-download these separately)
qemu/

# Screenshots / scratch
*.png
*.ppm

# Generated output from the dual-boot helper scripts (machine-specific, re-runnable)
preflight-out.txt
querymax-out.txt
maxshrink-out.txt
shrink-out.txt
unpool-out.txt
writeiso-out.txt
